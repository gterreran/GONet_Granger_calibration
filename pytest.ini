[pytest]
testpaths = tests
markers =
    dash: tests that require Dash components or callback modules
    realdata: optional tests that require a user-provided real calibration dataset
    slow: tests that may run expensive processing on real data
